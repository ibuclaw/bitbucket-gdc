import std.stdio: writeln;

void main(string[] args)
{
	if(args.length > 1) {
		if(args[1]=="one")
			writeln("Argument one received");
		else if(args[1]=="two")
			writeln("Argument two received");
		else
			writeln("I didn't recognize the argument you provided: ", args[1]);
	}
}
